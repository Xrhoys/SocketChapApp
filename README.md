# Experimental socket.io + React client in CodeSandbox

## Running the app

The backend should be runned by default.

Start the client by navigating in the frontend folder and run `npm start`.
